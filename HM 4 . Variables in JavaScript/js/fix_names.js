let age = 25;  // заменил x на age, чтобы было понятно, что это возраст
let name = 'Anna';  // заменил n на name, чтобы было понятно, что это имя
let userName = 'Bob';  // поменял 2userName на userName, потому что переменная не может начинаться с цифры
let userAge = 30;  // заменил user-age на userAge, потому что дефис нельзя использовать в имени переменной
let userFirstName = 'Charlie';  // заменил User_First_Name на userFirstName, чтобы имя было написано в camelCase
let userSecondName = 'Dmytro';  // заменил кириллицу на латиницу, чтобы имя переменной было написано правильно
let salary  = 100000;  // поменял a1 на salary, чтобы было понятно, что это зарплата
let userData = { age: 20 };  // заменил userdata на userData, чтобы имя было написано в camelCase


    console.log(age);
    console.log(name);
    console.log(userName);
    console.log(userAge);
    console.log(userFirstName);
    console.log(userSecondName);
    console.log(salary);
    console.log(userData);